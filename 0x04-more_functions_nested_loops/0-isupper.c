#include "main.h"
/**
 * _isupper -checks for uppercase letters
 * @x: char to check
 * Return: 0 or 1
 */
int _isupper(int x)
{
	if (x >= 'A' && c <= 'Z')
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
